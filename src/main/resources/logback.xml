<?xml version="1.0" encoding="UTF-8"?>

<configuration>
  <appender name="consoleAppender" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoders are assigned the type
         ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%t] %-5p %m%n</pattern>
    </encoder>
  </appender>

  <!-- To switch off just reflection's scan log completely -->
  <logger name="org.reflections" level="OFF" />
  
  <!-- To override the debug log level from the command line, use the
  VM option "-Dlogback.level.ai.timefold.solver=trace" -->
  <logger name="ai.timefold.solver" level="${logback.level.ai.timefold.solver:-info}"/>
  
  <!--
  Possible values for level="something" are: off, error, warn, info, debug, trace
  -->
  <root level="info">
    <appender-ref ref="consoleAppender" />
  </root>
</configuration>